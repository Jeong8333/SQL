SELECT info_no
     , nm
     , email
     , hobby
FROM tb_info
WHERE nm LIKE '±è%';

SELECT info_no
     , nm
     , email
     , hobby
FROM tb_info
WHERE nm LIKE '½Å%';

INSERT INTO tb_info (info_no, pc_no, nm, en_nm, email)
VALUES();

SELECT *
FROM tb_info;
